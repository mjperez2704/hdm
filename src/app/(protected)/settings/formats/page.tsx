import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function FormatsPage() {
  return (
      <Card>
        <CardHeader>
          <CardTitle>Formatos</CardTitle>
          <CardDescription>
            Administra la papelería y formatos de la empresa.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p>Próximamente: Módulo para gestionar formatos y documentos.</p>
        </CardContent>
      </Card>
  );
}
