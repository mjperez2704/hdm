import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function CustomReportsPage() {
  return (
      <Card>
        <CardHeader>
          <CardTitle>Reportes Personalizados</CardTitle>
          <CardDescription>
            Crea y guarda tus propios reportes personalizados.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p>Próximamente: Módulo de reportes personalizados.</p>
        </CardContent>
      </Card>
  );
}
